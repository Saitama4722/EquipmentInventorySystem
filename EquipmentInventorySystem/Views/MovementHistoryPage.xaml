<UserControl x:Class="EquipmentInventorySystem.Views.MovementHistoryPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Background="{StaticResource ContentBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ══ Page header ══ -->
        <StackPanel Grid.Row="0" Margin="24,22,24,14">
            <TextBlock Text="История перемещений" Style="{StaticResource PageTitleStyle}"/>
            <TextBlock Text="Журнал всех перемещений оборудования между помещениями и сотрудниками."
                       Style="{StaticResource PageSubtitleStyle}" Margin="0,3,0,0"/>
        </StackPanel>

        <!-- ══ Toolbar ══ -->
        <Border Grid.Row="1"
                Background="White"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="0,1,0,1"
                Padding="24,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Left: search -->
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBox x:Name="SearchBox"
                             Width="280"
                             Style="{StaticResource SearchBoxStyle}"
                             Tag="Поиск по наименованию или инв. номеру"
                             Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>

                <!-- Right: action buttons -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Content="Обновить"
                            Style="{StaticResource SecondaryButtonStyle}"
                            Click="RefreshButton_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- ══ DataGrid ══ -->
        <Border Grid.Row="2"
                Margin="24,16,24,16"
                Background="White"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="6"
                ClipToBounds="True">
            <DataGrid x:Name="HistoryGrid"
                      ItemsSource="{Binding Items}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      SelectionMode="Single"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      CanUserSortColumns="True"
                      GridLinesVisibility="Horizontal"
                      HorizontalGridLinesBrush="#F0F0F0"
                      AlternatingRowBackground="#FAFBFC"
                      BorderThickness="0"
                      RowHeight="36"
                      ColumnHeaderStyle="{StaticResource DataGridColumnHeaderStyle}"
                      RowStyle="{StaticResource DataGridRowStyle}">
                <DataGrid.Columns>

                    <DataGridTextColumn Header="Дата и время"
                                        Binding="{Binding MovedAtDisplay}"
                                        Width="130"
                                        SortMemberPath="MovedAt"
                                        ElementStyle="{StaticResource CellTextStyle}"/>

                    <DataGridTextColumn Header="Оборудование"
                                        Binding="{Binding EquipmentName}"
                                        Width="2*"
                                        ElementStyle="{StaticResource CellTextStyle}"/>

                    <DataGridTextColumn Header="Инв. №"
                                        Binding="{Binding InventoryNumber}"
                                        Width="100"
                                        ElementStyle="{StaticResource CellTextStyle}"/>

                    <DataGridTextColumn Header="Откуда (помещение)"
                                        Binding="{Binding FromRoomText}"
                                        Width="*"
                                        ElementStyle="{StaticResource CellTextStyle}"/>

                    <DataGridTextColumn Header="Куда (помещение)"
                                        Binding="{Binding ToRoomText}"
                                        Width="*"
                                        ElementStyle="{StaticResource CellTextStyle}"/>

                    <DataGridTextColumn Header="От сотрудника"
                                        Binding="{Binding FromEmployeeText}"
                                        Width="*"
                                        ElementStyle="{StaticResource CellTextStyle}"/>

                    <DataGridTextColumn Header="К сотруднику"
                                        Binding="{Binding ToEmployeeText}"
                                        Width="*"
                                        ElementStyle="{StaticResource CellTextStyle}"/>

                    <DataGridTextColumn Header="Что изменилось"
                                        Binding="{Binding ChangeDescription}"
                                        Width="155"
                                        ElementStyle="{StaticResource CellTextStyle}"/>

                    <DataGridTextColumn Header="Примечание"
                                        Binding="{Binding CommentText}"
                                        Width="*"
                                        ElementStyle="{StaticResource CellTextStyle}"/>

                </DataGrid.Columns>
            </DataGrid>
        </Border>

    </Grid>
</UserControl>
