<UserControl x:Class="EquipmentInventorySystem.Views.RoomsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Background="{StaticResource ContentBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ══ Page header ══ -->
        <StackPanel Grid.Row="0" Margin="24,22,24,14">
            <TextBlock Text="Помещения" Style="{StaticResource PageTitleStyle}"/>
            <TextBlock Text="Структура помещений и размещения оборудования."
                       Style="{StaticResource PageSubtitleStyle}" Margin="0,3,0,0"/>
        </StackPanel>

        <!-- ══ Toolbar ══ -->
        <Border Grid.Row="1"
                Background="White"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="0,1,0,1"
                Padding="24,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Left: search -->
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBox x:Name="SearchBox"
                             Width="260"
                             Style="{StaticResource SearchBoxStyle}"
                             Tag="Поиск по номеру или наименованию"
                             KeyDown="SearchBox_KeyDown"/>
                    <Button Content="Найти"
                            Margin="8,0,0,0"
                            Style="{StaticResource SecondaryButtonStyle}"
                            Click="SearchButton_Click"/>
                    <Button Content="Сброс"
                            Margin="4,0,0,0"
                            Style="{StaticResource SecondaryButtonStyle}"
                            Click="ClearButton_Click"/>
                </StackPanel>

                <!-- Right: action buttons -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Content="Изменить"
                            Margin="0,0,6,0"
                            Style="{StaticResource SecondaryButtonStyle}"
                            Click="EditButton_Click"/>
                    <Button Content="Удалить"
                            Margin="0,0,6,0"
                            Style="{StaticResource DangerButtonStyle}"
                            Click="DeleteButton_Click"/>
                    <Button Content="Обновить"
                            Margin="0,0,6,0"
                            Style="{StaticResource SecondaryButtonStyle}"
                            Click="RefreshButton_Click"/>
                    <Button Content="＋  Добавить"
                            Style="{StaticResource PrimaryButtonStyle}"
                            Click="AddButton_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- ══ DataGrid ══ -->
        <Border Grid.Row="2"
                Margin="24,16,24,16"
                Background="White"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="6"
                ClipToBounds="True">
            <DataGrid x:Name="RoomsGrid"
                      ItemsSource="{Binding Items}"
                      SelectedItem="{Binding SelectedItem}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      SelectionMode="Single"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      GridLinesVisibility="Horizontal"
                      HorizontalGridLinesBrush="#F0F0F0"
                      AlternatingRowBackground="#FAFBFC"
                      BorderThickness="0"
                      RowHeight="36"
                      ColumnHeaderStyle="{StaticResource DataGridColumnHeaderStyle}"
                      RowStyle="{StaticResource DataGridRowStyle}"
                      MouseDoubleClick="RoomsGrid_MouseDoubleClick">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Номер"
                                        Binding="{Binding Number}"
                                        Width="100"
                                        ElementStyle="{StaticResource CellTextStyle}"/>
                    <DataGridTextColumn Header="Наименование"
                                        Binding="{Binding Name}"
                                        Width="*"
                                        ElementStyle="{StaticResource CellTextStyle}"/>
                    <DataGridTextColumn Header="Описание"
                                        Binding="{Binding Description}"
                                        Width="2*"
                                        ElementStyle="{StaticResource CellTextStyle}"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

    </Grid>
</UserControl>
